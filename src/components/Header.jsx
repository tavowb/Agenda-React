import React from "react";

const Header = () => {
  return (
    <nav class="navbar navbar-dark bg-dark">
      <div className="container-fluid">
        <span class="navbar-brand mb-0 h1">Agenda react + Reducer</span>
      </div>
    </nav>
  );
};

export default Header;
